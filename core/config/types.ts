export const CONFIG = {
    pgSqlConfig: Symbol.for('pgSqlConfig'),
    CasesConfig: Symbol.for('CasesConfig'),
};

export const INFRA = {
    Databaste: Symbol.for('Database'),
    HttpClient: Symbol.for('HttpClient'),
};

export const DOMAIN = {
    CasesService: Symbol.for('CasesService'),
};